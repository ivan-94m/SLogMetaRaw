# SLogMetaRaw
> Sony camera metadata and Camera Raw-style controls for DaVinci Resolve 21. Reads XAVC MP4/MXF acquisition metadata > (ISO, white balance, lens, S-Log) into the Media Pool, plus a self-configuring OpenFX node for the Color page. macOS, GPL-3.
